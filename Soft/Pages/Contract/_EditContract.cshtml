@using Core.Extensions
@model PageModels.ContractModel

@Html.Select(model => model.item.personId, Model.Persons, "Select person")
@Html.Select(model => model.item.occupationId, Model.Occupations, "Select occupation")
@Html.Select(model => model.item.departmentId, Model.Departments, "Select department")
@Html.Editor(model => model.item.validFrom)
@Html.Editor(model => model.item.validTo)
