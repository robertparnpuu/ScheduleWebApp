@page
@using Core.Extensions
@model PageModels.OccupationModel

@{
    Layout = "_Index";
}

<table class="table">
    <thead>
        <tr>
            @Html.TableSortHeader(x => x.item.name, Model)
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.items)
        {
        <tr>
            @Html.RowData(x => item.name)
            @Html.RowButtons(Model.PageUrl, $"{item.id}")
        </tr>
        }
    </tbody>

</table>
